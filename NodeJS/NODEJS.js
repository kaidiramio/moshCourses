// NODE JS 

// NODE JS - open source runtime enviornment (JS ENGINE) to run Javascript outside of the browser. Node used to build back end services (APIS). 

// POSITIVES OF NODE: is ideal for building highly scalable, data-intensive, real-time apps and I/O intentsive apps (network access). Uses JavaScript so you can transistion to a back-end job, clear and more consistent codebase, large ecosystem of open source libraries. 

// NOT GOOD FOR CPU- intensive lots of calculations needed by CPU

// NODE is Asynchronous - non-blocking; single thread used to handle multiple requests

// Synchronous = blocking ; thread is used to handle request - threads have to wait until request is done/executed to process next request. Ex: clients have to wait 

// Node has Event Qeue - 